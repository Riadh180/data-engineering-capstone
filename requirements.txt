# --- core pipeline ---
pandas>=2.0
numpy>=1.24
sentence-transformers>=2.2      # ESCO semantic crosswalk (multilingual-e5)
pydriller>=2.5                  # churn signals (git history)
 
# --- serving / dashboard ---
streamlit>=1.36
plotly>=5.20
 
# --- warehouse (Postgres serving layer) ---
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0              # auto-load .env (GOLD_BACKEND, PG*)dbt-postgres>=1.7
s3fs
