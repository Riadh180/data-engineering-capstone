aiwork:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('PGHOST', 'localhost') }}"
      port: "{{ env_var('PGPORT', '5432') | int }}"
      user: "{{ env_var('PGUSER', 'aiwork') }}"
      password: "{{ env_var('PGPASSWORD', 'aiwork') }}"
      dbname: "{{ env_var('PGDATABASE', 'aiwork') }}"
      schema: gold
      sslmode: "{{ env_var('PGSSLMODE', 'prefer') }}"
      keepalives_idle: 30
      threads: 4